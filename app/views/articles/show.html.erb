<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<ul>
    <%# Need to pass in the article that is going to be edited/deleted %>
    <li> <%= link_to "Edit", edit_article_path(@article) %> </li>
    <li> <%= link_to "Destroy", article_path(@article),  data:{
                                                                turbo_method: :delete,
                                                                turbo_confirm: 'Are you sure?'
                                                            } 
        %> </li>
</ul>